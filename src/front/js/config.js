const WS_URL = "wss://projeto-sorteio.onrender.com";

const STATUS = {
  WIN: "venceu",
  LOSE: "perdeu",
}

const ACTIONS = {
  ADMIN: "admin",
  READY: "ready",
  CLIENT_COUNT_UPDATE: "clientCountUpdate",
}